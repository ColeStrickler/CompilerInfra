func : (b: bool) -> int {
	a : int = 4;
    a++;
    a--;

    a = a/4;
    a = a*4;
    a = a+4;

    x : bool;
    toconsole -a;
    x = !b;
    return a;
}




main : (x: int, z : bool) -> void {

    myVar : int = func(z); 
    x = myVar - 7;
}